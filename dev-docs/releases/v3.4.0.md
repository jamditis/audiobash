# AudioBash v3.4.0 release evidence

Status: Work in progress.

This document records the evidence used to prepare AudioBash v3.4.0. It does not contain passwords, private keys, API key identifiers, issuer identifiers, or certificate archives.

## Release goal

Ship smaller and more stable macOS arm64 and x64 packages. Both packages must use a supported Electron release, pass the test and static-analysis gates, carry a Developer ID signature with a secure timestamp, pass Apple notarization, include a stapled ticket, and pass Gatekeeper checks before publication.

## Release-gate evidence

| Check | Result | Evidence |
| --- | --- | --- |
| Apple Developer Program membership | Pass | The signed-in account and Apple notarization service accepted the dedicated team credential. |
| Developer ID private key | Pass | A local timestamped hardened-runtime signing probe passed. |
| Developer ID certificate | Pass | The certificate expires on February 1, 2027. |
| Issuing certificate chain | Pass | The current certificate uses Apple Developer ID G1. The G1 intermediate is installed and trusted. |
| Future certificate renewal | Pending before February 1, 2027 | A replacement certificate must use the current Apple intermediate. Apple Developer ID G2 is not required for the current G1 certificate. |
| Notarization credential | Pass | A dedicated Developer-role App Store Connect team API key passed `xcrun notarytool history`. |
| CI signing secrets | Pass | GitHub Actions lists `CSC_LINK` and `CSC_KEY_PASSWORD`. Values cannot be read back. |
| CI notarization secrets | Pass | GitHub Actions lists `APPLE_API_KEY`, `APPLE_API_KEY_ID`, `APPLE_API_ISSUER`, and `APPLE_TEAM_ID`. Values cannot be read back. |
| Temporary credential files | Blocking: pending user approval | The exact owner-only holding directory contains recoverable private-key exports and signing probes. Permanent removal needs explicit user approval and must finish before release. |

The selected release method is GitHub Actions signing with an encrypted PKCS #12 archive and notarization with a dedicated App Store Connect team API key. An ad hoc-signed v3.4.0 release is not permitted.

## Baseline environment

Captured at `2026-08-27T18:29:35Z` before product changes.

| Item | Value |
| --- | --- |
| Branch | `release/v3.4.0-macos-stability` |
| Commit | `a96935ab764c76259ebf5ea10c724b6c3b1f2855` |
| Describe | `v3.3.1-1-ga96935a` |
| Node.js | `v22.17.1` |
| npm | `10.9.2` |
| Python | `3.11.2` |
| Ruff | `0.15.1` |
| Xcode | `26.6` (`17F113`) |
| macOS | `26.6.2` |
| Architecture | `arm64` |
| Developer ID identity | Present |
| Six required Apple GitHub secrets | Present |

Ruff was installed with:

```sh
uv tool install 'ruff==0.15.1'
```

## Baseline results

| Command | Exit | Result |
| --- | ---: | --- |
| `npm ci` | 0 | Installed 716 packages. npm reported 32 findings. |
| `npm test` | 1 | 7 failed, 448 passed, and 3 skipped across 458 tests. |
| `npm run lint` | 0 | 0 errors and 65 warnings. |
| `npm run lint:py` | 0 | Passed with Ruff 0.15.1. |
| `npm run typecheck` | 0 | Passed. |
| `npm run format:check` | 0 | Passed. |
| `npm run build` | 0 | Passed. The initial JavaScript chunk was 1,088.55 kB, or 298.59 kB gzip. |
| `npm audit --omit=dev` | 1 | One moderate `protobufjs` finding. |
| `npm audit` | 1 | 1 low, 3 moderate, 23 high, and 5 critical findings. |

All seven test failures are in `tests/macos-stress.test.ts`. A clean install leaves both macOS `node-pty` `spawn-helper` files at mode `0644`. The permission assertions fail, and five real PTY tests fail with `posix_spawnp failed`.

## Task 2 red-green evidence

The native dependency repair has these results:

| Check | Red result | Green result |
| --- | --- | --- |
| New repair unit suite | `npx vitest run tests/unit/nodePtyBinaries.test.ts` failed because `scripts/nodePtyBinaries.cjs` did not exist. | Seven unit tests pass for arm64 and x64 repair, missing files, invalid permissions, an injected permission-operation failure, signing failure, and verification failure. |
| Packaged hook behavior | The initial five-case behavior suite failed because `createAfterPack` did not exist. | Ten behavior tests prove the configured export, real and injected Electron architecture mapping, the exact packaged native root, one target repair, non-macOS return, unsupported-architecture rejection, release mode, and fail-closed error propagation. |
| Required package hooks | The first focused package-script test failed because `prepare:native` was undefined. The expanded test then failed because `pretest:watch` was undefined. | The test passes for `prepare:native`, all 11 direct test and Electron lifecycle hooks, and the absence of `postinstall`. |
| Clean local native files | After `npm ci`, both macOS helpers had mode `0644`. Seven macOS stress tests failed, including five PTY operations with `posix_spawnp failed`. | `npm run prepare:native` changed both helpers to `0755`. Strict signature verification passed for both helpers and both `pty.node` files. The current macOS stress suite passes 14 tests and skips 3 package-only tests. |
| Controlled regression probe | With the arm64 helper restored to `0644`, the resize PTY test failed with `posix_spawnp failed`. | After another `npm run prepare:native`, the same PTY test passed. |
| Focused Task 2 gate | Not applicable. | `npx vitest run tests/unit/nodePtyBinaries.test.ts tests/macos-stress.test.ts` passed 21 tests and skipped 3 package-only tests. |
| Full test suite | The baseline had 7 failures. | `npm test` passed 468 tests and skipped 3 package-only tests across 22 files. |

The repair is explicit. It is not an npm `postinstall` hook. Non-macOS hosts do no native work. macOS test, Electron development, and Electron build entry points run it before they start. The package hook repairs only the target architecture and throws if permission repair, signing, or verification fails.

After the fix, lint passed with the same 65 baseline warnings and no errors. Type checking, the format check, and the renderer build passed. The renderer build size did not change.

## Local raw evidence

The ignored `.audit/` directory contains the raw command output for this session:

- `environment.txt`
- `npm-ci.txt`
- `npm-test.txt`
- `npm-lint.txt`
- `npm-lint-py.txt`
- `npm-typecheck.txt`
- `npm-format-check.txt`
- `npm-build.txt`
- `npm-audit-production.txt`
- `npm-audit-full.txt`

These files are local diagnostics. They are not release artifacts and are not committed.

Task 2 adds these ignored final-gate logs. Each file records its command and exit code:

- `task-2-node-syntax.txt`
- `task-2-prepare-native.txt`
- `task-2-native-verification.txt`
- `task-2-focused-tests.txt`
- `task-2-full-test.txt`
- `task-2-typecheck.txt`
- `task-2-lint.txt`
- `task-2-format-check.txt`
- `task-2-build.txt`
- `task-2-diff-check.txt`

## Review log

- The implementation plan received two independent Claude Code reviews before approval.
- A post-credential Claude Code review found that recoverable temporary private-key exports cannot be marked as cleaned up. The release gate remains pending until the user approves permanent removal.
- A split execution review clarified the Electron target, native-preparation hooks, retry bounds, warning budget, password variable, ignored audit evidence, and release filename source.
- Task 2 passed independent specification review and code-quality re-review. Claude Code found one remaining Electron architecture-contract test gap; the added real-enum test passed, and Claude Code re-review returned no findings.
- Each behavior change requires a failing test, a passing focused test, full validation, line-by-line review, and an independent review before commit.
